<Window x:Class="LogisticsWPF.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="300" Width="400"
        Background="{DynamicResource BackgroundColor}" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid>
        <Border Width="350" Height="250" Background="White" CornerRadius="10" Padding="20"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center" >
                <TextBlock Text="Авторизация" FontSize="22" FontWeight="Bold" 
                           Foreground="{DynamicResource PrimaryColor}" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <TextBlock Text="Логин:" Margin="0,0,0,5"/>
                <TextBox x:Name="LoginTextBox" Height="28" Margin="0,0,0,10"/>

                <TextBlock Text="Пароль:" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" Height="28" Margin="0,0,0,20"/>

                <Button Content="Войти" Height="35" Click="LoginButton_Click"
                        Background="{DynamicResource AccentColor}" Foreground="White"
                        FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>