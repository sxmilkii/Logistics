<Window x:Class="LogisticsWPF.Windows.ProductionOrderStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Смена статуса заказа"
        Width="350"
        Height="233"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <StackPanel Margin="20">
        <TextBlock Text="Текущий статус:" FontWeight="Bold"/>
        <TextBlock x:Name="CurrentStatusText" Margin="0,0,0,10"/>

        <TextBlock Text="Выберите новый статус:" FontWeight="Bold"/>
        <ComboBox x:Name="StatusComboBox" DisplayMemberPath="Name" SelectedValuePath="ProductionOrderStatusID" Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Width="100" Height="25" Click="Save_Click"/>
            <Button Content="Отмена" Width="100" Height="25" Margin="10,0,0,0" Click="Cancel_Click"/>
        </StackPanel>
    </StackPanel>
</Window>